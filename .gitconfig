[core]
  excludesfile = /Users/diederik/.gitignore

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
  required = true

[user]
	name = Diederik van den Burger
